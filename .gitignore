node_modules/
# Pinned upstream checkouts (scripts/fetch-sources.sh) and libvips build
# intermediates. Partly root-owned — remove with scripts/clean.sh, not rm -rf.
.work/
dist/
# The two emitted binaries. Not committed here: they are consumed by copying
# them into a project's tools/ (PhotoStructure commits them via git-LFS).
out/
# build transcript from scripts/build-all.sh
build*.log
